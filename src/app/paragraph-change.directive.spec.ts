import { ParagraphChangeDirective } from './paragraph-change.directive';

describe('ParagraphChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ParagraphChangeDirective();
    expect(directive).toBeTruthy();
  });
});
